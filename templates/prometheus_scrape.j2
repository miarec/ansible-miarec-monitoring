  - job_name: prometheus
    static_configs:
      - targets:
          - "localhost:{{ prometheus_listen_port }}"

  - job_name: node
    static_configs:
      - targets:
        - "localhost:{{ node_exporter_listen_port }}"

  - job_name: process
    static_configs:
      - targets:
        - "localhost:{{ process_exporter_listen_port }}"
